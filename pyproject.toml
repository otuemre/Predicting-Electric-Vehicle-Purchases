[project]
name = "predicting-electric-vehicle-purchases"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "otuemre", email = "dev.emre17@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.2",
    "numpy>=2.4.6",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.1",
    "seaborn>=0.13.2",
]

[project.scripts]
predicting-electric-vehicle-purchases = "predicting_electric_vehicle_purchases:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
